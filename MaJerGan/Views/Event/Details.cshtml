@* @model MaJerGan.Models.Event

@{
    ViewData["Title"] = "รายละเอียดกิจกรรม";
}

<h2>📌 รายละเอียดกิจกรรม</h2>

<div class="card">
    <div class="card-body">
        <h3>@Model.Title</h3>
        <p>จำนวนเข้าชม: @Model.ViewCount views</p>
        <p><strong>รายละเอียด:</strong> @Model.Description</p>
        <p><strong>แท็ก:</strong> @Model.Tags</p>
        <p><strong>วันที่จัด:</strong> @Model.EventTime.ToString("dd/MM/yyyy HH:mm")</p>
        <p><strong>ปิดรับสมัคร:</strong> @Model.ExpiryDate.ToString("dd/MM/yyyy")</p>
        <p><strong>จำนวนผู้เข้าร่วมสูงสุด:</strong> @Model.MaxParticipants </p>
        <p><strong>ข้อมูลเพิ่มเติม:</strong> @Model.ExtraInfo</p>
        <p><strong>สร้างเมื่อ:</strong> @Model.CreatedAt</p>
        <p><strong>สร้างโดย:</strong> @Model.Creator.Username</p>

        <a href="@Url.Action("Index", "Event")" class="btn btn-primary">🔙 กลับไปหน้าหลัก</a>
    </div>
</div> *@

<h2>@Model.Title</h2>
<p>สร้างโดย: @Model.Creator.Username</p>
<p>จำนวนเข้าชม: @Model.ViewCount views</p>

<h3>ผู้เข้าร่วม</h3>
<ul>
    @foreach (var participant in Model.Participants)
    {
        <li>
            @participant.User.Username (@participant.JoinedAt.ToShortDateString())
            @if (participant.Status == 0)
            {
                <span style="color: orange;">(รออนุมัติ)</span>
            }
            else
            {
                <span style="color: green;">(อนุมัติแล้ว)</span>
            }
        </li>
    }
</ul>
